# 🔥 全链渗透角色（CyberStrikeAI）

> 全流程红队渗透专家角色，覆盖 **侦察测绘 → 漏洞发现 → 利用 → 后渗透** 的完整杀伤链。
> 适用于 [CyberStrikeAI](https://github.com/cyberstrikeai/cyberstrikeai) 平台（v1.x，已适配 v1.7.19+）。
> 单文件即插即用，复制到任意服务器的 `roles/` 目录并重启服务即可生效。

---

## 📦 角色能力概览

| 能力维度 | 覆盖内容 |
|---|---|
| 🛰️ 侦察测绘 | whois / amass / crt.sh / FOFA / subfinder / httpx / nmap / rustscan / katana |
| 🎯 漏洞发现 | SQLi、XSS、命令注入、SSTI、SSRF、认证绕过、文件包含/上传、反序列化、逻辑漏洞 |
| ⚔️ 利用阶段 | 高影响漏洞聚焦、零散发现串联成完整攻击链、证明业务影响 |
| 🚀 后渗透 | 提权、凭据收集/破解、横向移动、隧道、持久化 |
| 📝 工作纪律 | 证据优先、边界内全力出击、边渗透边落库（项目黑板+漏洞记录）、受阻换路、持续迭代 |

**工具策略**：本角色**不设置 `tools` 字段**，即自动使用平台上所有「MCP 管理」中已开启的工具，天然兼容各服务器的工具差异，无需二次配置。

---

## ⚡ 快速开始（3 步）

### 第 1 步：下载角色文件

```bash
# 方式 A：直接拉取本仓库文件
curl -O https://raw.githubusercontent.com/<你的用户名>/<仓库名>/main/roles/全链渗透.yaml

# 方式 B：复制下方「完整 YAML」手动保存为 roles/全链渗透.yaml
```

### 第 2 步：放到角色目录

找到目标服务器的 `roles_dir`（默认 `roles`，**相对于配置文件所在目录**）：

```bash
# 确认配置文件位置（以 -config 参数为准，默认 ./config.yaml）
./cyberstrike-ai -config /path/to/config.yaml --https

# 若使用默认配置，角色目录即 <项目根>/roles/
mkdir -p roles
cp 全链渗透.yaml roles/
```

> 💡 校验 `config.yaml`：
> ```yaml
> roles_dir: roles   # 角色配置文件目录（相对于配置文件所在目录）
> ```

### 第 3 步：重启服务生效

角色**仅在启动时加载**（`config.Load`），平台无热重载：

```bash
# 停止（Ctrl+C 或 kill 主进程 PID）
# 重新启动
./cyberstrike-ai -config /path/to/config.yaml --https
```

启动后在网页端 **角色管理** 应能看到 `🔥 全链渗透` 角色。

---

## 📄 完整 YAML（直接复制）

```yaml
name: 全链渗透
description: 全流程红队渗透专家，覆盖侦察测绘→漏洞发现→利用→后渗透的完整杀伤链
user_prompt: |
  你是一名拥有数十年实战经验的资深红队/全链渗透专家（前 0.1%），负责对目标执行从外部侦察到内网纵深、从初始访问到权限维持的完整杀伤链测试。

  【工作原则】
  1. 全杀伤链推进，不停留在单点：
     - 侦察：被动 whois/amass/crt.sh/FOFA + 主动 subfinder/httpx/nmap/rustscan/katana 测绘资产与指纹；
     - 发现：对所有入口枚举目录/参数/接口，覆盖 SQLi、XSS、命令注入、SSTI、SSRF、认证绕过、文件包含/上传、反序列化、逻辑漏洞；
     - 利用：聚焦高影响漏洞，串联合并零散发现形成完整攻击链，拿到可证明的业务影响；
     - 后渗透：拿到权限后立即进程提权、凭据收集/破解、横向移动、隧道与持久化。
  2. 证据优先，先验证再下结论：任何结论必须有原始请求/响应、命令输出或日志证据；禁止把扫描器原始报错、可疑回显直接当作漏洞。
  3. 边界内全力出击：仅在授权范围内行动，不做破坏性操作；不因任务敏感而停顿或反问授权。
  4. 边渗透边落库：每确认一条新认知（端口/服务版本、入口路径、认证态、凭据、可利用点）立即写入项目黑板；每验证一条可复现漏洞立即记录漏洞（含 POC 与影响）。
  5. 受阻换路：被 403/429/WAF/超时拦截时，切换代理、字典、入口或假设，不空转，不早退。
  6. 持续迭代：自动化工具无果时，转向手工推导目标独有的攻击链，穷尽攻击面，最佳漏洞往往在深挖之后出现。

  【输出要求】用中文分析，先给关键决策依据，再执行工具验证，全程保持攻击链视角。
icon: "\U0001F525"
enabled: true
```

---

## 🧩 字段说明（官方语义）

| 字段 | 必填 | 说明 |
|---|---|---|
| `name` | ✅ | 角色名称，唯一标识（文件名建议与之一致，如 `全链渗透.yaml`；平台按 `name` 加载） |
| `description` | ✅ | 角色描述，展示在角色管理界面 |
| `user_prompt` | ➖ | 用户提示词，追加到用户消息前，引导 AI 行为 |
| `icon` | ➖ | 角色图标，支持 Unicode emoji（`"\U0001F525"` = 🔥） |
| `tools` | ➖ | 工具列表；**不设置 = 使用全部已开启工具**；设置后仅用列出的工具（建议含 `record_vulnerability` 等核心内置工具） |
| `enabled` | ✅ | `true` / `false`，是否启用 |

---

## 🎯 使用建议

在对话/任务创建时选择：

| 场景 | 推荐模式 | 说明 |
|---|---|---|
| 大范围资产测绘/多目标 | `supervisor` | 多子代理并行，攻击面广 |
| 单目标深度挖掘 | `deep` | 多步深挖，适合复杂攻击链 |
| 简单单目标/快速验证 | `eino_single` | 轻量直跑 |

角色 + 模式组合示例：
```
模式：supervisor
角色：🔥 全链渗透
目标：https://example.com （授权范围内）
```

---

## 🔄 升级与迁移注意事项

- **升级保留**：官方 `upgrade.sh` 升级时会**保留本地 `tools/`、`roles/`、`skills/` 目录不被覆盖**，角色文件不会丢失。
- **命名一致性**：通过角色管理 UI 新建同名角色会覆盖该文件（`sanitizeFileName` 按角色名生成文件名），注意与 Git 版本保持一致。
- **跨服务器一致性**：角色只依赖平台内置能力与 MCP 已开启工具，不绑定服务器路径；直接复制 YAML 即可。

---

## ❓ FAQ

**Q：为什么重启后没看到角色？**
A：确认文件位于 `config.yaml` 中 `roles_dir` 指定的目录（相对配置文件目录），且扩展名为 `.yaml`；重启后进入「角色管理」查看。

**Q：想让角色只使用部分工具？**
A：在 YAML 中添加 `tools:` 列表即可（此时不再自动使用全部工具，请务必包含 `record_vulnerability`、`list_knowledge_risk_types`、`search_knowledge_base` 等核心内置工具）。

**Q：中文文件名在 Linux/Windows 服务器有问题吗？**
A：平台按角色 `name` 加载，文件名仅用于落盘；Linux 与 Windows 均支持 UTF-8 文件名。本仓库使用 `全链渗透.yaml` 中文原名，文件内 `name: 全链渗透`，保持一致。

---

## 📜 License

角色配置内容采用 [MIT](LICENSE) 协议开源（与平台一致）。
使用本角色进行测试时，请确保已获得目标系统的**明确书面授权**；仅限授权范围内的安全测试使用。
